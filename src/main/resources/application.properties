spring.application.name=Abyss

spring.docker.compose.enabled=false

# 1. Connection Details
spring.datasource.url=jdbc:postgresql://localhost:5432/abyss
spring.datasource.username=postgres
# ^ Change to 'abyss_user' if you created one
spring.datasource.password=Miki.234

# 2. Hibernate Settings (The Magic)
# 'update' automatically creates tables (users, identities) based on your Java entities
spring.jpa.hibernate.ddl-auto=create

# 3. Debugging (Optional)
# Shows the raw SQL commands in your console
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect